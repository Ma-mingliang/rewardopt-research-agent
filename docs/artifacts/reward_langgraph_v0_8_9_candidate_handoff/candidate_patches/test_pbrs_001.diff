--- a/env.py
+++ b/env.py
@@ -995,2 +995,7 @@
         reward = tracking_reward + bonus_reward + smoothness_penalty + improvement_reward + action_penalty + residual_penalty + subgoal_reward
+        # Risk penalty: large penalty for high angular velocity or large error
+        risk_penalty = 0.0
+        if angular_velocity > 1.5 or current_error > 0.3:
+            risk_penalty = -3.0
+        reward += risk_penalty